# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug
*.log

# Local config
.env
.env.local

# claude-tower data
.claude
.superpowers/
# Agent working notes: plans, provisional design docs, screen captures. The
# reasoning worth keeping goes in Backlog or a commit message, not here.
docs/superpowers/

# Test dependencies (installed via git clone)
tests/bats/

# Test temporary files
tests/tmp/

# Agent-generated audit/analysis reports. These are intermediate artifacts —
# the same scan re-run under a new filename — and have repeatedly littered the
# repo root. Keep findings worth acting on in Backlog, not in dropped JSON.
SECURITY_*.json
security-*.json
*_ANALYSIS.json
*_AUDIT.json
*_FINDINGS.json


# Removed Windows wrapper files per user request
hive-mind-prompt-*.txt

# Claude Flow generated files
.claude/settings.local.json
.mcp.json
claude-flow.config.json
.swarm/
.hive-mind/
.claude-flow/
memory/
coordination/
memory/claude-flow-data.json
memory/sessions/*
!memory/sessions/README.md
memory/agents/*
!memory/agents/README.md
coordination/memory_bank/*
coordination/subtasks/*
coordination/orchestration/*
*.db
*.db-journal
*.db-wal
*.sqlite
*.sqlite-journal
*.sqlite-wal
claude-flow
# Removed Windows wrapper files per user request
hive-mind-prompt-*.txt
